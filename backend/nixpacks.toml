[phases.setup]
nixPkgs = ["python310"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["python manage.py migrate", "python manage.py seed_data"]

[start]
cmd = "gunicorn config.wsgi:application --bind 0.0.0.0:$PORT"
